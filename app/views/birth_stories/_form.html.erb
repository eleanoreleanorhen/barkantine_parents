<%= form_for(@birth_story) do |f| %>
  <% if @birth_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@birth_story.errors.count, "error") %> prohibited this birth_story from being saved:</h2>

      <ul>
      <% @birth_story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

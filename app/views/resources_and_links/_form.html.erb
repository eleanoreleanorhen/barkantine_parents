<%= form_for(@resources_and_link) do |f| %>
  <% if @resources_and_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resources_and_link.errors.count, "error") %> prohibited this resources_and_link from being saved:</h2>

      <ul>
      <% @resources_and_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
